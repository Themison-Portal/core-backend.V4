[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
addopts = -v --tb=short
markers =
    cloud: live integration tests that hit the deployed Cloud Run instance (require CLOUD_* env vars)
