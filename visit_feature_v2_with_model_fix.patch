diff --git a/app/clients/rag_client.py b/app/clients/rag_client.py
index b3a2164..c010864 100644
--- a/app/clients/rag_client.py
+++ b/app/clients/rag_client.py
@@ -55,15 +55,23 @@ class RagClient:
     async def _ensure_connected(self):
         """Ensure gRPC channel is connected."""
         if self._channel is None:
-            self._channel = aio.insecure_channel(
-                self.address,
-                options=[
-                    ("grpc.max_send_message_length", 100 * 1024 * 1024),
-                    ("grpc.max_receive_message_length", 100 * 1024 * 1024),
-                ],
-            )
+            options = [
+                ("grpc.max_send_message_length", 100 * 1024 * 1024),
+                ("grpc.max_receive_message_length", 100 * 1024 * 1024),
+            ]
+            
+            # Strip https:// or http:// if present, as grpc channel doesn't use it
+            address_cleaned = self.address.replace("https://", "").replace("http://", "")
+            
+            # Use secure channel for Cloud Run or when port is 443
+            if "run.app" in address_cleaned or ":443" in address_cleaned:
+                credentials = grpc.ssl_channel_credentials()
+                self._channel = aio.secure_channel(address_cleaned, credentials, options=options)
+            else:
+                self._channel = aio.insecure_channel(address_cleaned, options=options)
+                
             self._stub = RagServiceStub(self._channel)
-            logger.info(f"Connected to RAG Service at {self.address}")
+            logger.info(f"Connected to RAG Service at {self.address} ({'secure' if 'run.app' in address_cleaned else 'insecure'})")
 
     async def close(self):
         """Close the gRPC channel."""
diff --git a/app/main.py b/app/main.py
index 5a823f0..94f25af 100644
--- a/app/main.py
+++ b/app/main.py
@@ -13,6 +13,9 @@ from app.api.routes.auth import router as auth_router
 from app.api.routes.query import router as query_router
 from app.api.routes.upload import router as upload_router
 
+# Ensure all models are loaded in correct order into the mapper registry
+import app.models
+
 # Storage routes
 from app.api.routes.storage.storage import router as storage_router
 from app.api.routes.local_files import router as local_files_router
diff --git a/app/models/__init__.py b/app/models/__init__.py
index 4d33566..dd2c1f5 100644
--- a/app/models/__init__.py
+++ b/app/models/__init__.py
@@ -41,8 +41,8 @@ from .patient_documents import PatientDocument
 
 # Tier 5
 from .patient_visits import PatientVisit
-from .qa_repository import QARepositoryItem
-
-# Tier 6
 from .visit_documents import VisitDocument
 from .visit_activities import VisitActivity
+from .qa_repository import QARepositoryItem
+
+
